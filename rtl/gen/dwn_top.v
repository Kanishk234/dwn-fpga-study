// GENERATED by exporter/emit_encoder.py -- do not edit by hand.
//
// Combinational end to end: quantized features in, class index out. No pipeline
// registers yet -- brief §9 wants a register between stages for II=1, and
// pipeline depth is a Phase 2 sweep axis, so stages get placed once synthesis
// shows where the critical path actually is (very likely the comparators).

`timescale 1ns / 1ps
`default_nettype none

module dwn_top (
    input  wire [255:0] x_flat,
    output wire [2:0] class_idx
);

    wire [3199:0] bits;

    thermometer_encoder u_enc (.x_flat(x_flat), .bits(bits));
    dwn_core            u_core (.x(bits),       .class_idx(class_idx));

endmodule

`default_nettype wire
